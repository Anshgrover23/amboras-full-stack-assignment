# Backend (NestJS)

All setup steps, environment variables, API overview, and architecture notes are in the **[root README](../README.md)**.

Quick start from repo root: `cd backend`, copy `../.env.example` to `.env`, then `bun install`, `bun run seed`, `bun run start:dev`.
