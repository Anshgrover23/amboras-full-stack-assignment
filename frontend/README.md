# Frontend (Next.js)

All setup steps, environment variables, and architecture notes are in the **[root README](../README.md)**.

Quick start from repo root: `cd frontend`, copy `../.env.example` to `.env.local`, set `NEXT_PUBLIC_API_URL`, then `bun install`, `bun run dev` (app on port **3001**).
